#include <stdio.h>

int parse(char *str, char *substr)
{
    int count = 0, len_src = strlen(str), len_sub = strlen(substr);
    char *tmp;
}

int main(int argc, const char *argv[])
{
    
    return 0;
}
